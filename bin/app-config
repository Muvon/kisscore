#!/usr/bin/env bash
if [[ -z $1 || -z $2 ]]; then
  echo 'Usage: app-config block params'
  exit 1
fi

if [[ -z $PROJECT ]]; then
  echo 'Not in project. Run kiss PROJECT';
  exit 1
fi

echo "
[$1]
$2
" >> $APP_DIR/config/app.ini.tpl
