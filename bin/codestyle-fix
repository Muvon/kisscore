#!/usr/bin/env bash
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd -P )"
PROJECT_DIR="$(cd "$SCRIPT_DIR/.." && pwd -P )"
cd "$PROJECT_DIR" || exit 1
vendor/bin/phpcbf -v -s --standard=ruleset.xml app/
