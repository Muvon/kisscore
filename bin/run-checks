#!/usr/bin/env bash
exit_code=0

for cmd in "$@"; do
  eval $cmd &> /dev/null
  if [[ "$?" != "0" ]]; then
    echo -e "$cmd - \e[1;31mFail\e[0;0m"
    exit_code=1
  else
    echo -e "$cmd - \e[1;32mSuccess\e[0;0m"
  fi
done

exit $exit_code
