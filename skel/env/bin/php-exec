#!/usr/bin/php
<?php
include getenv('KISS_CORE');

if (!isset($_SERVER['argv'][1])) {
  throw new Exception('You must write code to execute');
}

// Получаем ответ и выполняем запрос
App::start();
eval($_SERVER['argv'][1] . ';');
echo PHP_EOL;
App::stop();
