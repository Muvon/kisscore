#!/bin/bash
declare -A checks

checks['tar']='tar --version'
checks['xargs']='xargs --version'
checks['realpath']='realpath --version'
checks['nohup']='nohup --version'
checks['nano']='nano --version'
checks['crontab']='which crontab'

run_checks