#!/usr/bin/env bash
declare -a checks=(
  'which tar'
  'which xargs'
  'which nano'
  'which sed'
  'which tr'
  'which php'
  'php -r "version_compare(PHP_VERSION, '"'5.6'"') > 0 && exit(0) || exit(1);"'
)

run-checks "${checks[@]}"
